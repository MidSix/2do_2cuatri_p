### 1ra pregunta
![[Pasted image 20260308110027.png]]
![[Pasted image 20260308110854.png]]

### 2da pregunta

![[Pasted image 20260308110101.png]]

![[Pasted image 20260308110935.png]]

- Aqui el error estuvo en que mi hipotesis dadas las condiciones del problema NO puede concluirse si es falsa o verdadera y en consecuencia eso NO es evaluable(pero el razonamiento y la conclusion en caso que la hipotesis fuera cierta serian validos para explicar el problema). YO infiero que la CPU se trata de una monocore o duo core ya que al intentar paralelizar con mas cores se nota como hay un aumento en los tiempos y por tanto una reduccion en el rendimiento, digo que esto se debe a limitaciones hardware por la que crear mas cores de los que la CPU puede manejar de forma paralela desemboca en un context switching costoso y crear procesos innecesarios, digo que eso ralentiza el proceso y es cierto. Pero no me centre en el fallo global sino en un fallo especifico. La razon "conocida" y que se puede inferir de la imagen, por la que falla el paralelismo NO la toque, en su lugar solo toque un fallo "probable" que no puede inferirse de la imagen dada. Me arme una hipotesis que, de ser cierta, explica de forma CORRECTA los resultados, el problema no es la conclusion ni el razonamiento a partir de la hipotesis sino la hipotesis en si misma. **Por que la hipotesis NO puede comprobarse y de hacerlo y resultar verdadera TAMPOCO seria la que ralentice mas, y por tanto aquella razon mas importante, y que por tanto ES la que debe ser mencionada en la respuesta?**

- La clave esta en las cargas: COUNT_INI y COUNT_MAX, 1e2 y 1e3 respectivamente. Son cargas demasiado bajas para las cuales NO renta en ningun contexto aplicar paralelismo debido al overhead -> [[002_overhead_programacion_paralela]]. Entonces la verdadera razon por la que aplicar paralelismo en este contexto NO es rentable es debido al Overhead del paralelismo, hay un minimo de tareas a partir de las cuales el paralelismo se convierte en una buena idea y ese "minimo" no lo alcanzamos con cargas tan pequeñas. **Por que mi hipotesis no puede evaluarse?** Para confirmar o desmentir que un ordenador tiene un numero determinado de cores dada la imagen planteada, es necesario **Un numero suficiente da tareas/carga** y de resultar eso cierto entonces PRO_2 deberia ser mas veloz que SEQ. Luego veriamos el punto a partir del cual los tiempos dejan de bajar y empiezan a subir, encontrar el optimo y demas y con eso podriamos inferir el numero de cores del ordenador. Pero vemos que PRO_2 es mucho mas ineficiente que SEQ en el problema dado, eso significa que ningun nivel de paralelismo es util, si eso ocurre es o porque el procesador es monocore o el nivel de carga es insuficiente. Como la hipotesis de que sea monocore a partir de la imagen puede plantearse como probable pero no confirmarse y la de bajas cargas sí se confirma, pues esa es la razón correcta. Una carga de trabajo insuficiente es la razon por la que aplicar paralelismo NO funciona, **no se supera el overhead del paralelismo**.

- En este caso, con 1e2 y 1e3 iteraciones, el cálculo es tan trivial que el "Tiempo Útil" es casi cero. Por lo tanto, lo que estás midiendo en la tabla no es el rendimiento de los cores de la CPU procesando datos, estás midiendo cuánto tarda el sistema operativo en levantar y gestionar el pool de procesos.
     
-   **Por tanto, no puedes deducir el hardware si el límite primario es el software**. Como dices, para confirmar tu hipótesis, la imagen tendría que mostrar cargas pesadas (ej. 1e7). Si ahí PRO_2 fuera mejor que SEQ, pero PRO_4 fuera peor, entonces SÍ tendrías la prueba irrefutable de que estás ante una máquina de 2 cores.
### 3ra pregunta

![[Pasted image 20260308103953.png]]

![[Pasted image 20260308103831.png]]

- Para responder correctamente hay que explicar que se hace linea por linea. Mientras se hace el examen el approach que puede seguirse es explicar que se hace linea a linea por lo mas probable que haga.

```python
# Inicializa una count dandole el valor de una constante COUNT_INI definida en alguna otra parte del codigo
count = COUNT_INI

# Crea un bucle que se repitira mientras count sea menor o igual que la constante COUNT_MAX
while count <= COUNT_MAX:

# Se llama a la funcion sci_not_str y se le pasa como como argumento count (Aqui toca inferir que pasa xd, ese nombre significa -> scientific notation to string) entonces simplemente se le pasa count y te lo devuelve en notacion cientifica y lo guarda en N
N = sci_not_str(count)

# Se indexa df, que es un dataframe que debe estar definido en alguna otra parte del codigo. Se usa su meotod loc para indexar, seleccionamos la fila N y todas sus columnas, o sea toda la fila N. Y hacemos un grafico de barras con el titulo dado. ".plot" instancia un objeto tipo Figure que se guarda en global y se le da el nombre de plt, por eso luego usamos metodos de plt y nos estamos refiriendo a la Figure que creamos aqui usando *.plot(...)
df.loc[N, :].plot(kind='bar', title="Time in milliseconds for N={0}.format(N))")

#️ Simplemente rotamos las etiquetas del eje de las x para tambien aprovechar espacio vertical, esto lo hacemos para evitar que se superpongan las etiquetas por falta de espacio
plt.xticks(rotation=45)

# Es un gestor de geometria que setea automaticamente el layout por nosotros, asi no nos tenemos que preocupar por setear los elementos en pantalla especificando pixeles, por ello al hacer esto conseguimos todos un layout comun.
plt.tight_layout()

# Con esto guardamos la figura especificando el nombre y extension.
plt.savefig('{0}/{1}'.format(experiment_label,sci_not_str(count)))

# La figura creada queda en memoria, es una buena practica liberarla.
plt.close()

# Redefinimos count para 1-> No tener un bucle infinito y 2-> escoger una fila distinta para hacer el grafico en cada iteracion. Lo hacemos con una constante llamada COUNT_STEP.
count = count * COUNT_STEP
```

- Se encarga de mostrar en un gráfico de barras los datos devueltos por el programa. Su uso principal es facilitar el encuentro del punto optimo(aquel punto donde se minimizan los tiempos de ejecución con el paralelismo).